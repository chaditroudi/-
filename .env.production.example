# Copy this to .env.production (never commit .env.production to git)
# Then set VITE_API_URL as a GitHub Actions secret for the CI build.

# Your hosted NestJS backend URL (Railway / Render / VPS / etc.)
VITE_API_URL=https://your-backend.com/api
