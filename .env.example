MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_DB_NAME=date_harvest_hub
JWT_SECRET=replace-with-a-long-random-secret
PORT=4000
API_HOST=0.0.0.0
DATABASE_RETRY_INTERVAL_MS=60000
VITE_API_URL=/api

# Wave B mass-balance gate: off | warn (default) | enforce
# Flip to enforce only after backfill:mass-balance and smoke:wave-b are green.
# TRUST_MASS_BALANCE_GATE=warn
# TRUST_MASS_BALANCE_TOLERANCE_PCT=2
